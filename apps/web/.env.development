# Chỉ áp cho `next dev` — Next tự nạp file này, không cần thêm gì.
# Không chứa bí mật: đây là cờ hiển thị, và giá trị mặc định của nó là TẮT.

# Hiện danh sách tài khoản demo trên trang đăng nhập.
# 🔒 Ở production, biến này không được đặt -> khối đó không render.
NEXT_PUBLIC_DEMO_HINTS=1
