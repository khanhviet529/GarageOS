// Cấu hình gốc — mọi package dùng chung một bộ quy tắc.
// Xem packages/config/eslint.config.js để biết vì sao quy tắc INV-T-02 nằm ở đó.
export { default } from './packages/config/eslint.config.js';
